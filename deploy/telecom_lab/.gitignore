# Thư mục staging CSV trước khi đẩy lên object storage — sinh lại được, không commit.
.stage/
# File môi trường thật.
.env
# Output của script verify/benchmark.
reports/

# Chua jwtToken cua OpenMetadata; chi commit ban .tpl
trino_ingestion.yaml
